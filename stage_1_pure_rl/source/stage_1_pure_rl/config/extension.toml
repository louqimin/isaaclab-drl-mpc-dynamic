[package]

# Semantic Versioning is used: https://semver.org/
version = "0.1.0"

# Description
category = "isaaclab"
readme  = "README.md"

title = "Stage 1: Pure RL Baseline — Anymal D Rough Terrain Locomotion"
author = "louqimin"
maintainer = "louqimin"
description="Stage 1 of isaaclab-drl-mpc-dynamic: PPO baseline for Anymal D rough terrain locomotion using rsl_rl."
repository = "https://github.com/louqimin/isaaclab-drl-mpc-dynamic.git"
keywords = ["isaaclab", "anymal", "locomotion", "rsl_rl", "ppo", "stage1"]

[dependencies]
"isaaclab" = {}
"isaaclab_assets" = {}
"isaaclab_mimic" = {}
"isaaclab_rl" = {}
"isaaclab_tasks" = {}
# NOTE: Add additional dependencies here

[[python.module]]
name = "stage_1_pure_rl"

[isaac_lab_settings]
# TODO: Uncomment and list any apt dependencies here.
#       If none, leave it commented out.
# apt_deps = ["example_package"]
# TODO: Uncomment and provide path to a ros_ws
#       with rosdeps to be installed. If none,
#       leave it commented out.
# ros_ws = "path/from/extension_root/to/ros_ws"
